import 'package:flutter/material.dart';

class DepartmentInputField extends StatelessWidget {
  const DepartmentInputField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
